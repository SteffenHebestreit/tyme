# Test Environment Configuration
# This file is used when running tests

NODE_ENV=test

# Test Database Configuration
# When running inside Docker: use container name
# When running from host: use localhost with mapped port
DATABASE_URL=postgresql://postgres:testpass@localhost:5434/tyme_test
DB_HOST=localhost
DB_PORT=5434
DB_USER=postgres
DB_PASSWORD=testpass
DB_NAME=tyme_test

# Test-specific settings
JWT_SECRET=test-jwt-secret-for-testing-only
SECRET_KEY=test-secret-key-for-testing-only

# Disable external services during tests
SMTP_HOST=
REDIS_URL=
