# Test Environment Configuration for Docker
# Used when tests run inside a container in the same Docker network

NODE_ENV=test

# Test Database Configuration - using Docker service name
DATABASE_URL=postgresql://postgres:testpass@tyme-test-db:5432/tyme_test
DB_HOST=tyme-test-db
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=testpass
DB_NAME=tyme_test

# Test-specific settings
JWT_SECRET=test-jwt-secret-for-testing-only
SECRET_KEY=test-secret-key-for-testing-only

# Disable external services during tests
SMTP_HOST=
REDIS_URL=
